{
  "name": "portfolio-mcp-server",
  "version": "1.0.0",
  "description": "Stdio MCP server for Tom Sloan's portfolio (Claude Desktop integration)",
  "type": "module",
  "scripts": {
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "devDependencies": {
    "netlify-cli": "^17.37.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
